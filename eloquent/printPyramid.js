function drawPyramid(width)
{
	var height = width / 2;
	for (var y = 0; y < height; ++y) {
		for (var x = 0; x < width; ++x) {

		}
	}
}

// Wrapper for text output function
function print(string)
{
	process.stdout.write(string);
}
